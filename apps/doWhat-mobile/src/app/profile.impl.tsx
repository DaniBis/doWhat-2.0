// Inert stub: legacy profile implementation removed.
// Prevent expo-router from treating this as a screen.
if (__DEV__) console.warn('[profile.impl.tsx] legacy stub loaded – safe to delete.');
export default function RemovedProfileImpl() { return null; }
