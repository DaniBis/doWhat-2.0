{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @dowhat/shared build && pnpm --filter dowhat-web run build",
  "crons": [
    {
      "path": "/api/cron/places/bangkok",
      "schedule": "0 3 * * *"
    }
  ]
}
